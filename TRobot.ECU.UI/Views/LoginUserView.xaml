<Window x:Class="TRobot.ECU.UI.Views.LoginUserView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:validators="clr-namespace:TRobot.Core.UI.Validators;assembly=TRobot.Core.UI"
        xmlns:local="clr-namespace:TRobot.ECU.UI"
        mc:Ignorable="d"
        Title="Login"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight">
    <StackPanel>
        <GroupBox Header="User" Margin="5,5,5,5" >
            <StackPanel >
                <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch">
                    <Label  Style="{StaticResource Label}" HorizontalAlignment="Left" Margin="5,5,0,5">
                        User name:
                    </Label>
                    <TextBox  Text="{Binding LoginUserModel.UserName, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" Style="{StaticResource TextBox}" Margin="5,5,5,5" Width="100" VerticalAlignment="Top">
                        <Validation.ErrorTemplate>
                            <ControlTemplate>
                                <StackPanel>
                                    <!-- Placeholder for the TextBox itself -->
                                    <AdornedElementPlaceholder />
                                    <ItemsControl ItemsSource="{Binding}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding ErrorContent.ValidationMessage}" >
                                                    <TextBlock.Style>
                                                        <Style TargetType="{x:Type TextBlock}">
                                                            <Setter Property="Foreground" Value="Red"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ErrorContent.Severity}" Value="{x:Static local:Severity.WARNING}">
                                                                    <Setter Property="Foreground" Value="Orange"/>
                                                                </DataTrigger>                                                                
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </ControlTemplate>
                        </Validation.ErrorTemplate>
                    </TextBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label  Style="{StaticResource Label}" HorizontalAlignment="Left" Margin="5,5,0,5">
                        Password:
                    </Label>
                    <PasswordBox Name="PasswordBox" Margin="12,5,5,5" Width="100" HorizontalAlignment="Center">
                    </PasswordBox>
                </StackPanel>
            </StackPanel>
        </GroupBox>
        <StackPanel Orientation="Horizontal"  Margin="0,0,0,0" HorizontalAlignment="Right">
            <Button Style="{StaticResource Button}" Command="{Binding Path=OKCommand}" >
                OK
                <Button.ToolTip>
                    <TextBlock>OK</TextBlock>
                </Button.ToolTip>
            </Button>
            <Button Style="{StaticResource Button}" Command="{Binding Path=CancelCommand}" Margin="5, 5, 5, 5">
                Cancel
                <Button.ToolTip>
                    <TextBlock>Cancel</TextBlock>
                </Button.ToolTip>
            </Button>
        </StackPanel>
    </StackPanel>
</Window>
