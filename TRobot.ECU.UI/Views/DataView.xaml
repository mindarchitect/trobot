<Window x:Class="TRobot.ECU.UI.Views.DataView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TRobot.ECU.UI.Views"
        mc:Ignorable="d"
        Title="DataView" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="Button" TargetType="{x:Type Button}">
            <Setter Property="Margin" Value="5,5,5,5" />
            <Setter Property="Padding" Value="5,5,5,5" />
            <Setter Property="MinWidth" Value="80" />
            <Setter Property="MaxWidth" Value="200" />
        </Style>
    </Window.Resources>

    <StackPanel>
        <Button Style="{StaticResource Button}" Content="Button" HorizontalAlignment="Left" Margin="576,161,0,0" VerticalAlignment="Top" Width="75" Command="{Binding Path=GetDataCommand}" />
    </StackPanel>
</Window>
